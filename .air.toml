root = "."
testdata_dir = "testdata"
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/agentcontainer ./cmd/agentcontainer"
delay = 1000
entrypoint = ["./tmp/agentcontainer"]
exclude_dir = ["assets", "tmp", "vendor", "testdata", "prd"]
exclude_regex = ["_test\\.go"]
